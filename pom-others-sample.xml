<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>eu.europa.ec.taxud.uumds-init-env</groupId>
		<artifactId>all</artifactId>
		<version>0.1.2-SNAPSHOT</version>
	</parent>

	<artifactId>uumds-init-dev-env</artifactId>
	<url>${module.relative.url}</url>
	<properties>
		<uumds.admin.persistence.version>0.3.4</uumds.admin.persistence.version>
		<uumds.pdp.version>0.3.5</uumds.pdp.version>
	</properties>
	<dependencies>
		<dependency>
			<groupId>eu.europa.ec.taxud.uumds-init-env</groupId>
			<artifactId>uumds-init-env-common</artifactId>
		</dependency>
		<dependency>
			<groupId>eu.europa.ec.taxud</groupId>
			<artifactId>uumds-ejb-pdp-service</artifactId>
			<version>${uumds.pdp.version}</version>
		</dependency>
		<!-- <dependency> <groupId>eu.europa.ec.taxud</groupId> <artifactId>uumds-ejb-pdp-service</artifactId> 
			<version>${uumds.pdp.version}</version> <classifier>test</classifier> </dependency> -->
		<dependency>
			<groupId>eu.europa.ec.taxud</groupId>
			<artifactId>uumds-pdp-test</artifactId>
			<version>${uumds.pdp.version}</version>
		</dependency>
		<dependency>
			<groupId>eu.europa.ec.taxud</groupId>
			<artifactId>uumds-ejb-persistence-reference-data</artifactId>
			<version>${uumds.admin.persistence.version}</version>
		</dependency>
		<dependency>
			<groupId>eu.europa.ec.taxud</groupId>
			<artifactId>uumds-ejb-persistence-authorization-data</artifactId>
			<version>${uumds.admin.persistence.version}</version>
		</dependency>
		<dependency>
			<groupId>eu.europa.ec.taxud</groupId>
			<artifactId>uumds-ejb-persistence-identity-data</artifactId>
			<version>${uumds.admin.persistence.version}</version>
		</dependency>
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<addClasspath>true</addClasspath>
							<mainClass>eu.europa.ec.taxud.uumds.init.InitDevEnv</mainClass>
                            <classpathPrefix>WEB-INF/lib/</classpathPrefix>
                            <!-- NOTE: Deprecated in version 2.4. Use 'classpathLayoutType' instead.
                            <classpathMavenRepositoryLayout>true</classpathMavenRepositoryLayout>
                            <classpathLayoutType>repository</classpathLayoutType>
                            -->
						</manifest>
					</archive>
                </configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>attached</goal>
						</goals>
						<phase>package</phase>
						<configuration>
                            <descriptors>
                                <descriptor>assembly-dependency-lib.xml</descriptor>
                            </descriptors>
                            <!--
                            <descriptorRefs>
								<descriptorRef>jar-with-dependencies</descriptorRef>
							</descriptorRefs>
                            -->
							<archive>
								<manifest>
                                    <addClasspath>true</addClasspath>
                                    <mainClass>eu.europa.ec.taxud.uumds.init.InitDevEnv</mainClass>
                                    <classpathPrefix>lib/</classpathPrefix>
								</manifest>
							</archive>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<!-- <plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>maven-dependency-plugin</artifactId> 
				<executions> <execution> <id>copy-dependencies</id> <phase>prepare-package</phase> 
				<goals> <goal>copy-dependencies</goal> </goals> <configuration> <outputDirectory>${project.build.directory}/lib</outputDirectory> 
				<overWriteReleases>false</overWriteReleases> <overWriteSnapshots>false</overWriteSnapshots> 
				<overWriteIfNewer>true</overWriteIfNewer> </configuration> </execution> </executions> 
				</plugin> <plugin> <groupId>org.apache.maven.plugins</groupId> <artifactId>maven-jar-plugin</artifactId> 
				<configuration> <archive> <manifest> <addClasspath>true</addClasspath> <classpathPrefix>lib/</classpathPrefix> 
				<mainClass>eu.europa.ec.taxud.uumds.init.InitDevEnv</mainClass> </manifest> 
				</archive> </configuration> </plugin> -->
<!-- 			<plugin>
				see proguard options at http://proguard.sourceforge.net/manual/usage.html
				<groupId>com.github.wvengen</groupId>
				<artifactId>proguard-maven-plugin</artifactId>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>proguard</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<obfuscate>false</obfuscate>
					<outjar>${project.build.finalName}-out.jar</outjar>
					<libs>
						<lib>${java.home}/lib/rt.jar</lib>
						<lib>${java.home}/lib/jsse.jar</lib>
						<lib>${java.home}/lib/jce.jar</lib>
					</libs>
					<includeDependencyInjar>true</includeDependencyInjar>
					<addMavenDescriptor>true</addMavenDescriptor>
					<options>
						don't do anything
						<option>-dontpreverify</option>
						<option>-dontoptimize</option>
						<option>-dontshrink</option>

						<option>-ignorewarnings</option>
						<option>-defaultpackage ''</option>
						<option>-allowaccessmodification</option>
						<option>-verbose</option>
						<option>-printmapping
							${project.build.directory}/proguard-mapping.txt</option>
						<option>-printusage ${project.build.directory}/proguard-usage.txt</option>
						<option>-printseeds ${project.build.directory}/proguard-seeds.txt</option>
						<option>-skipnonpubliclibraryclasses</option>
					</options>
					<option>-shrink</option> overloadaggressively="false" optimize="false" 
						allowaccessmodification="false" skipnonpubliclibraryclasses="false" repackageclasses=""
				</configuration>
			</plugin>
 -->		</plugins>
	</build>
</project>